---
interface Props {
	title: string;
	description: string;
}

const { title, description } = Astro.props;
// 💡 关键：获取当前页面所属的语言（'en' 或 'zh' 或 'de'）
const currentLang = Astro.currentLocale || 'en';
---
<style is:global>
@import "tailwindcss";
</style>

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		
	</head>
	<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col">
		<header class="bg-white border-b border-slate-200 py-4 px-6 shadow-sm">
			<div class="max-w-6xl mx-auto flex justify-between items-center">
				<a href="/" class="text-xl font-bold text-indigo-600 flex items-center gap-2">
					⚡ NanoSVG Tools
				</a>
<nav class="flex items-center gap-6 text-sm text-slate-600">
					<a href="#features" class="hover:text-indigo-600">Features</a>
					<a href="#faq" class="hover:text-indigo-600">FAQ</a>
					
					<div class="relative inline-block text-left border-l border-slate-200 pl-4">
						<select 
							onchange="location = this.value;" 
							class="bg-slate-50 border border-slate-300 text-slate-700 py-1 px-2 rounded-md text-xs font-medium focus:outline-none focus:ring-1 focus:ring-indigo-500 cursor-pointer"
						>
							<option value="/" selected={currentLang === 'en'}>🇺🇸 English</option>
							<option value="/zh/" selected={currentLang === 'zh'}>🇨🇳 简体中文</option>
							<option value="/de/" selected={currentLang === 'de'}>🇩🇪 Deutsch</option>
						</select>
					</div>
				</nav>
			</div>
		</header>

		<!-- 核心内容区 + 响应式双翼/侧边广告栏布局 -->
		<main class="flex-1 max-w-7xl w-full mx-auto px-4 py-8 grid grid-cols-1 lg:grid-cols-4 gap-8">
			
			<!-- 左侧/中间：工具核心与SEO文章 (占 3 个网格) -->
			<div class="lg:col-span-3 flex flex-col gap-8">
				<slot />
			</div>

		
      <aside class="hidden lg:block lg:col-span-1">
	<div class="sticky top-8 w-[300px] bg-white border border-slate-200 rounded-2xl p-6 flex flex-col gap-4 shadow-sm">
		<div class="flex items-center gap-2 text-indigo-600 font-bold text-sm">
			<span>🔒 Privacy Guarantee</span>
		</div>
		<p class="text-xs text-slate-500 leading-relaxed">
			NanoSVG Tools is built with a serverless architecture. Your vector files and source codes never leave your local machine, ensuring complete data sovereignty.
		</p>
		<div class="border-t border-slate-100 pt-3 flex flex-col gap-2">
			<span class="text-[11px] font-semibold text-slate-400 uppercase tracking-wider">Open Source</span>
			<a href="https://github.com" target="_blank" class="text-xs text-slate-600 hover:text-indigo-600 flex items-center gap-1">
				⭐ Star on GitHub →
			</a>
		</div>
	</div>
</aside>
		</main>

		<footer class="bg-slate-900 text-slate-400 text-xs py-6 border-t border-slate-800 mt-12 text-center">
			<p>© 2026 NanoSVG Tools. 100% Client-Side & Privacy-First.</p>
		</footer>
	</body>
</html>

